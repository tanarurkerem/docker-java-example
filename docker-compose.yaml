#mvn "-Duser.home=/var/maven" clean install
version: "3.8"
services:
  console:
    image: maven
    volumes:
      - ./:/usr/src/mymaven
      - ./.m2:/var/maven/.m2
    working_dir: /usr/src/mymaven
    environment:
      MAVEN_CONFIG: /var/maven/.m2
    user: "1000"